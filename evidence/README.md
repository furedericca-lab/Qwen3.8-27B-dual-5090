# Evidence

Tracked evidence is limited to immutable model identity. Generated benchmark, probe, long-context, and soak outputs are intentionally ignored. Preserve a selected acceptance run externally before pruning local evidence.

Every accepted result must identify the model SHA256, llama.cpp SHA, kernel, driver, CUDA version, context, KV type, fit target, batch, and ubatch.
